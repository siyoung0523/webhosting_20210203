#include <stdio.h>

void MaxAndMin(int* maxPtr, int* minPtr){
    int temp = 0;
    if(*minPtr>*maxPtr){

    }

}

void main(){
    int arr[5];
    int* maxPtr;
    maxPtr = &arr[0];

    int* minPtr;
    minPtr = &arr[0];
    
    int i = 0, a = 0, b = 0;

    for(;i<5;i++){
        scanf("%d", &a);
        arr[i] = a;
    }

    MaxAndMin()
    {

    }
    

}